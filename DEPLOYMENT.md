# Deployment Guide - CreatorConnectAI

This guide will help you deploy CreatorConnectAI to production using Vercel (frontend) and Render (backend).

---

## Prerequisites

1. GitHub account with your repository
2. Vercel account (sign up at https://vercel.com)
3. Render account (sign up at https://render.com)
4. Firebase project with Firestore enabled
5. Groq API key (for AI features)

---

## Part 1: Deploy Backend to Render

### Step 1: Create New Web Service

1. Go to https://render.com/dashboard
2. Click "New +" → "Web Service"
3. Connect your GitHub repository: `Narayan1006/Creatorconnectai`
4. Render will auto-detect the `render.yaml` configuration

### Step 2: Configure Environment Variables

In the Render dashboard, add these environment variables:

**Required:**
- `FIREBASE_PROJECT_ID` = Your Firebase project ID
- `GROQ_API_KEY` = Your Groq API key
- `JWT_SECRET` = (auto-generated by Render)
- `AUTH_SECRET` = (auto-generated by Render)

**Optional:**
- `OPENAI_API_KEY` = Your OpenAI key (if using OpenAI instead of Groq)

### Step 3: Upload Firebase Credentials

1. In Render dashboard, go to your service
2. Navigate to "Environment" tab
3. Add a secret file:
   - Name: `firebase-service-account.json`
   - Content: Paste your Firebase service account JSON

### Step 4: Deploy

1. Click "Create Web Service"
2. Wait for deployment (5-10 minutes)
3. Note your backend URL: `https://creatorconnectai-backend.onrender.com`

---

## Part 2: Deploy Frontend to Vercel

### Step 1: Import Project

1. Go to https://vercel.com/dashboard
2. Click "Add New..." → "Project"
3. Import your GitHub repository: `Narayan1006/Creatorconnectai`
4. Vercel will auto-detect it's a Vite project

### Step 2: Configure Build Settings

**Framework Preset:** Vite
**Root Directory:** `frontend`
**Build Command:** `npm run build`
**Output Directory:** `dist`

### Step 3: Add Environment Variable

Add this environment variable in Vercel:

- `VITE_API_URL` = Your Render backend URL (e.g., `https://creatorconnectai-backend.onrender.com`)

### Step 4: Deploy

1. Click "Deploy"
2. Wait for deployment (2-3 minutes)
3. Your frontend will be live at: `https://creatorconnectai.vercel.app`

---

## Part 3: Update CORS Settings

After deployment, update the backend CORS settings:

1. Edit `backend/app/main.py`
2. Update the `allow_origins` list:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:5173",
        "http://localhost:3000",
        "https://creatorconnectai.vercel.app",  # Add your Vercel URL
        "https://*.vercel.app"  # Allow all Vercel preview deployments
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

3. Commit and push changes
4. Render will auto-deploy the update

---

## Part 4: Verify Deployment

### Test Backend

Visit: `https://your-backend-url.onrender.com/health`

Expected response:
```json
{"status": "ok"}
```

### Test Frontend

1. Visit your Vercel URL
2. Try registering a new user
3. Create a creator profile
4. Test the matching system

---

## Troubleshooting

### Backend Issues

**Problem:** Service won't start
- Check Render logs for errors
- Verify all environment variables are set
- Ensure Firebase credentials are uploaded correctly

**Problem:** FAISS index errors
- The disk storage is configured in `render.yaml`
- Check that the mount path is correct

**Problem:** Slow cold starts
- Render free tier has cold starts (~30 seconds)
- Upgrade to paid plan for always-on instances

### Frontend Issues

**Problem:** API calls failing
- Check `VITE_API_URL` is set correctly
- Verify CORS settings in backend
- Check browser console for errors

**Problem:** Build fails
- Check Node.js version (should be 18+)
- Verify all dependencies are in `package.json`
- Check Vercel build logs

---

## Monitoring & Maintenance

### Render Dashboard
- Monitor backend health
- View logs
- Check resource usage
- Set up alerts

### Vercel Dashboard
- Monitor frontend deployments
- View analytics
- Check build logs
- Set up custom domain

---

## Scaling Considerations

### When to Upgrade

**Render:**
- Free tier: Good for testing (cold starts, limited hours)
- Starter ($7/month): Always-on, no cold starts
- Standard ($25/month): More resources, better performance

**Vercel:**
- Hobby (Free): Good for personal projects
- Pro ($20/month): Custom domains, more bandwidth
- Enterprise: High traffic, team features

### Performance Optimization

1. **Enable caching** for FAISS index
2. **Use Redis** for session management (Render add-on)
3. **Optimize images** in frontend
4. **Enable compression** in backend
5. **Use CDN** for static assets (Vercel does this automatically)

---

## Security Checklist

- [ ] Change default JWT secrets
- [ ] Enable HTTPS only (both platforms do this by default)
- [ ] Restrict CORS to your domains only
- [ ] Keep Firebase credentials secure (never commit to git)
- [ ] Enable rate limiting (add to backend)
- [ ] Set up monitoring and alerts
- [ ] Regular security updates for dependencies

---

## Cost Estimate

**Free Tier (Testing):**
- Render: Free (with limitations)
- Vercel: Free
- Firebase: Free tier (up to 50K reads/day)
- **Total: $0/month**

**Production (Recommended):**
- Render Starter: $7/month
- Vercel Pro: $20/month
- Firebase: ~$5-10/month (depending on usage)
- **Total: ~$32-37/month**

---

## Support

If you encounter issues:

1. Check Render logs: Dashboard → Your Service → Logs
2. Check Vercel logs: Dashboard → Your Project → Deployments → View Logs
3. Review Firebase console for database errors
4. Check GitHub Actions for CI/CD issues

---

## Next Steps

After successful deployment:

1. Set up custom domain (optional)
2. Configure email notifications
3. Set up monitoring (Sentry, LogRocket)
4. Enable analytics (Google Analytics, Mixpanel)
5. Set up automated backups for Firestore
6. Configure CI/CD for automated testing

---

**Deployment Date:** April 5, 2026
**Last Updated:** April 5, 2026
